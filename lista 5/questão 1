int maior (int x,int y)
{
    if(x>y) return x;
    else return y;
}
int main()
{
    int x, y, a;
    printf("Insira dois numeros: \n");
    scanf("%d %d", &x, &y);
    printf("O maior numero eh: %d", maior(x,y));
    return 0;
}
